import 'package:flutter/material.dart';

class Dietary {
  IconData icon;
  String label;
  bool isSelected;

  Dietary({
    required this.icon,
    required this.label,
    required this.isSelected,
  });
}
