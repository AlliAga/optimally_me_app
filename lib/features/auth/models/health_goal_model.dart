import 'package:flutter/material.dart';

class HealthGoal {
  IconData icon;
  String label;
  bool isSelected;

  HealthGoal({
    required this.icon,
    required this.label,
    required this.isSelected,
  });
}
